{
  "name": "chainstack-keepalive",
  "version": "1.0.0",
  "description": "Cloudflare Worker to keep Chainstack API alive with daily calls",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}